# 루티즈 (Routiz) — 지도 기반 하루 코스 설계 프로토타입

기능명세서(2026-08-20)와 로우파이 시안을 기준으로 만든 **모바일 웹앱 형태의 동작 프로토타입**입니다.
장소 검색 → 코스 편집 → 순서·동선 확인 → 저장·공유 → 공개 코스 탐색·신고까지 실제로 눌러가며 확인할 수 있습니다.

## 실행

```bash
npm install
cp .env.example .env.local   # Supabase URL·anon key 채우기 (supabase/README.md 참고)
npm run dev      # http://localhost:5173
npm run build    # 정적 배포용 번들 (dist/)
```

`.env.local`을 채우지 않으면 회원가입·로그인·DB 저장이 꺼진 오프라인 모드로 뜹니다 (공개 코스·저장 장소가 비어있는 채로 화면 구조만 확인 가능).

데스크톱 브라우저에서는 가운데 폰 프레임으로, 모바일에서는 전체 화면으로 표시됩니다.
개발자도구의 모바일 뷰(390×844)로 보면 시안과 가장 가깝습니다.

## 화면 구성

| 경로 | 화면 | 대응 기능 |
|---|---|---|
| `#/` | 내 코스 — 상단 2/3 지도 + 하단 1/3 가로 장소 카드, "순서 정하기"만 노출 | 2.1, 3.1 |
| `#/edit/:id` | 코스 편집 — 장소 분포 지도 + 장소 카드 | 1.1, 2.1 |
| `#/order/:id` | 순서 정하기 — 세로 카드 드래그 정렬, 구간 거리, 장소 추가·동선 확인·코스 저장 | 2.1, 4.1 |
| `#/summary/:id` | 동선 요약 — 구간별 이동수단·거리 | 3.1 |
| `#/search/:id` | 장소 검색 — 검색 또는 이미지 붙여넣기(캡처·텍스트) 두 경로, 결과는 지도+장소 카드 | 1.1, 7.1 |
| `#/save/:id` | 코스 저장 — 이름·설명·대표 이미지·공개 설정·공유 링크 | 5.1 |
| `#/s/:token` | 공유 링크로 열람 (읽기 전용) | 5.2 |
| `#/course/:id` | 공개 코스 상세 + 신고 | 6.2, 9.1 |
| `#/explore` | 공개 코스 탐색 — 지역·테마·이동수단 필터 | 9.1 |
| `#/saved` | 저장 장소 목록 — 상단 지도, 카테고리 필터, 카드별 메모 | 1.2 |
| `#/me` | 내 정보 — 로그인 상태, 내 코스, 설정 | 6.1 |
| `#/prefs/:id` | 선호 조건 점검 및 조정 제안 | 4.1 |
| `#/login` | 이메일 회원가입 / 로그인 | 6.1 |
| `#/onboarding` | 회원가입 직후 온보딩 — 선호 이동수단·장소 유형 선택 | 6.1 |
| `#/admin` | 신고 관리 — 숨김·삭제·기각 | 6.2 |

## 디자인

무채색 기본 팔레트입니다. 키컬러는 검정(`#111111`)이고, 배경은 항상 흰색, 카드·구획 테두리는 진한 회색(`#4d4d4d`)을 씁니다.

- **색**: canvas `#ffffff` · ink/key(텍스트·브랜드) `#111111` · foreground `#333333` · muted `#888888` · surface `#eeeeee` · border(카드 등 테두리) `#4d4d4d`
- **라운드**: 12(버튼) · 10(카드·배지) · 18(검색) · 24(시트·작은 버튼) · 30(필터 칩) · 999(핀·토글)
- **간격**: 4 / 6 / 8 / 16 / 20
- **타이포**: SUIT. 화면 타이틀 28/700, 앱바 17/700, 본문 14/400(21px), 읽기 본문 14/400(24.92px, -0.2px), 캡션 12/400(18px, -0.2px)
- **컴포넌트**: 필터 칩(surface·30px), 배지(10px), 검색 컨트롤(18px·44px), 하단 탭은 화면에 고정된 일반 탭바(플로팅 아님). 그림자 토큰은 쓰지 않고 border로만 층을 구분합니다.
- **아이콘 없음**: 모든 동작은 텍스트 레이블로 표시합니다. 지도 컨트롤(확대·축소는 `+`/`-`, 전체 보기만 지구본 아이콘), 상태 표시 등은 대부분 글자입니다.

폰트는 jsDelivr의 SUIT를 `@font-face`로 불러옵니다(`src/styles.css` 상단). CDN을 차단한 환경에서는 시스템 산세리프로 대체됩니다.

## 구현 범위와 대체 구현

프로토타입이므로 서버 없이 브라우저 안에서 동작합니다. 지도만 네이버 클라우드플랫폼(NCP) Dynamic Map을 실제로 불러옵니다.

- **지도**: 네이버 지도(NCP Maps) Dynamic Map SDK를 `src/lib/naverMaps.ts`에서 동적으로 로드합니다. Client ID는 웹 서비스 URL 제한으로 보호되는 공개 식별자라 코드에 포함돼 있고, Client Secret은 서버 간 REST 호출(geocoding 등)에만 쓰이므로 클라이언트 코드에는 넣지 않았습니다. 마커·경로선·구간 거리 라벨은 디자인 시스템 톤에 맞춘 커스텀 오버레이로 그립니다. NCP 콘솔의 **Web 서비스 URL**에 실행 도메인(예: `http://localhost:5173`, 배포 도메인)이 등록돼 있지 않으면 인증에 실패하며, 이 경우 지도 대신 안내 배너가 표시됩니다.
- **경로·이동거리**: 실제 길찾기 API 대신 하버사인 거리를 사용합니다. 이동수단(도보/대중교통/자동차) 선택은 유지하되, 시간 대신 거리(km) 기준으로 안내합니다. 도보 5km 초과 구간은 **경로 계산 불가**로 처리해 원인과 대안을 안내합니다.
- **장소 데이터**: 서울 주요 지역 40곳을 좌표·카테고리·영업시간과 함께 내장했습니다.
- **이미지 인식**: 캡처 이미지 업로드는 OCR을 흉내 내 후보를 반환하고, 텍스트 붙여넣기는 실제로 파싱해 내장 장소와 유사도(dice 계수)로 매칭합니다. 매칭이 불확실하면 직접 연결·제외할 수 있습니다.
- **인증·온보딩**: Supabase Auth 이메일/비밀번호 회원가입·로그인입니다(카카오·구글 없음). `.env.local`(`.env.example` 참고)에 `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`가 없으면 회원가입·로그인이 비활성화됩니다. 회원가입 후 선호 조건(`preferences`)을 한 번도 저장한 적 없는 계정은 자동으로 `#/onboarding`으로 이동해 이동수단·선호 장소 유형을 고르고("건너뛰기" 가능), 그 순간 DB에 선호 조건 행이 생기면서 다시 뜨지 않습니다.
- **저장소**: 실제 저장 데이터는 Supabase Postgres(`supabase/schema.sql`, RLS 포함)에 저장됩니다. 아직 "코스 저장"을 누르지 않은 편집 중인 초안만 로그인 여부와 상관없이 브라우저 `localStorage`에 남아 있다가, 저장하는 순간 로그인한 계정 명의로 DB에 올라갑니다. 내 정보 → *프로토타입 데이터 초기화*는 이 로컬 초안만 지우고 로그아웃합니다(DB에 이미 저장한 코스는 그대로 남습니다). 자세한 내용은 `supabase/README.md` 참고.

## 구조

```
src/
  lib/        types · geo(투영·경로) · schedule(시각·충돌·순서 제안) · course · store · router · supabase · db(Supabase 쿼리)
  data/       places(장소 40곳) · seed(기본 선호 조건 · 테마 목록)
  components/ MapCanvas · SortableList · 공통 UI
  screens/    화면별 컴포넌트
supabase/     schema.sql(테이블·RLS) · seed.sql · README.md
```
